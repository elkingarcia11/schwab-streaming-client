# Email Configuration
# Copy this file to .env and fill in your actual values

# SMTP Authentication (for sending emails)
EMAIL_ADDRESS=your-email@gmail.com
# For Gmail, use an App Password (not your regular password)
EMAIL_PASSWORD=your-app-password

# Sender Configuration (who the email appears to be from)
# If not set, will use EMAIL_ADDRESS as the sender email
SENDER_EMAIL=your-sender-email@example.com
# Examples:
# SENDER_EMAIL=trading-bot@yourdomain.com
# SENDER_EMAIL=notifications@yourcompany.com
# SENDER_EMAIL=alerts@yoursystem.com

# SMTP server settings
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Sender name (optional) - this will appear as the "From" name in emails
# If not set, will use the email address as the sender name
SENDER_NAME=Your Name
# Examples:
# SENDER_NAME=Schwab Trading Bot
# SENDER_NAME=John Doe
# SENDER_NAME=My Trading System

# Common SMTP Settings:
# Gmail: smtp.gmail.com:587 (requires App Password)
# Outlook: smtp-mail.outlook.com:587
# Yahoo: smtp.mail.yahoo.com:587

# Email recipients for trade notifications (comma-separated)
EMAIL_RECIPIENTS=your-email@example.com,another-email@example.com 